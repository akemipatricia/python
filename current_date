from datetime import date
today = date.today()
current_day = pd.to_numeric(today.strftime("%d"))
current_month = pd.to_numeric(today.strftime("%m"))
current_year = pd.to_numeric(today.strftime("%Y"))
